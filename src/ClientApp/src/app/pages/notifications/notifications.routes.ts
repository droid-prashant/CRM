import { Routes } from '@angular/router';
import { NotificationConfiguration } from './components/notification-configuration/notification-configuration';

export default [
    {
        path: '',
        component: NotificationConfiguration
    }
] as Routes;
